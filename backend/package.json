{
  "name": "hassan-ai-control-plane",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy:dry": "wrangler deploy --dry-run",
    "db:local": "wrangler d1 migrations apply hassan-ai --local"
  },
  "dependencies": {
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@types/node": "22.20.1",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.127.1"
  },
  "engines": {
    "node": ">=22"
  }
}
